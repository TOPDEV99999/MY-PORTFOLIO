[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[functions]
  # esbuild bundles the function and resolves ../../shared/ at deploy time
  node_bundler = "esbuild"
